package com.example.demo.service.match;

import com.example.demo.dto.MatchDto;
import com.example.demo.dto.MyMatchDto;

import java.time.LocalDate;
import java.util.List;
/*

public interface MatchService222 {
    List<MatchDto> getMatchDtoByDate(LocalDate date);

    List<MyMatchDto> getMyMatchByMemberIdAndDate(Long memberId, LocalDate day);

    List<MyMatchDto> getMyMatchByMemberIdAndDate(Long memberId, LocalDate startDay, LocalDate endDay);
}
*/
