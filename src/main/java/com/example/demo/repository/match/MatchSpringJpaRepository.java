package com.example.demo.repository.match;

import com.example.demo.domain.Match;
import org.springframework.data.jpa.repository.JpaRepository;

import java.time.LocalDate;
import java.util.List;
import java.util.Optional;

/*
public interface MatchSpringJpaRepository extends JpaRepository<Match, Long> {

    List<Match> findByDate(LocalDate date);

    Optional<Match> findById(Long id);

    List<Match> findByMemberIdAndDate(Long memberId, LocalDate date);

    Match findById(Integer id);
}
*/
